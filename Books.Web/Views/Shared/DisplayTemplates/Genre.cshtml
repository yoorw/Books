@*@using Books.Entities
@using Books.Web.Helpers
@{ 
    // Take that value and convert it to a user friendly string by looking 
    // up the display attributes
    var items = typeof(Genre).GetItems((int?)Model);
}*@

@*@Html.*@